name: Android Build

on: push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Set Up JDK
        uses: actions/set-java@v1
        with:
          java-version: 1.8

      - name: Run Test
        run: ./gradlew test

      - name: Build Project
        run: ./gradlew assemble