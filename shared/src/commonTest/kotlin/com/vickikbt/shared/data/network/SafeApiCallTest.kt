package com.vickikbt.shared.data.network

class SafeApiCallTest {

    /*private val apiService = ApiService(httpClient = MockServer.mockHttpClient)

    @Test
    fun `fetch now playing movies success returns NetworkResult Success`() = runBlocking {
        val networkResponse = safeApiCall { apiService.fetchNowPlayingMovies() }

        assertNotNull(actual = networkResponse)
        assertTrue(actual = networkResponse is NetworkResult.Success)
    }

    @Test
    fun `fetch now playing movies failure returns NetworkResult Error`() = runBlocking {
        MockServer.expectSuccess(isSuccess = false)

        val networkResponse = safeApiCall { apiService.fetchNowPlayingMovies() }

        assertNotNull(actual = networkResponse)
        assertTrue(actual = networkResponse is NetworkResult.Error)
    }*/
}
