<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.company.favorites">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_favorites">
        <dist:delivery>
            <dist:on-demand /> <!--ToDo: Revert back to on demand delivery-->
            <!--<dist:install-time />-->
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity android:name=".ui.activity.FavoritesActivity"/>
    </application>

</manifest>